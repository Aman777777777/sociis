const SectionFlywheel = () => {
  return (
    <section id="flywheel" className="py-20 sm:py-28">
      <div className="container mx-auto px-6 lg:px-8">
        <div className="max-w-[720px]">
          <p className="text-body-lg text-muted-foreground">
            Behaviour is tested in real environments, expressed through culture, and structured over time into a framework designed to become a global standard.
          </p>
        </div>
      </div>
    </section>
  );
};

export default SectionFlywheel;
